# To use, install prettier globally with pnpm
[[language]]
name = "html"
formatter = { command = 'prettier', args = ["--parser", "html"] }

[[language]]
name = "json"
formatter = { command = 'prettier', args = ["--parser", "json"] }

[[language]]
name = "css"
formatter = { command = 'prettier', args = ["--parser", "css"] }

[[language]]
name = "javascript"
formatter = { command = 'prettier', args = ["--parser", "typescript"] }
auto-format = true

[[language]]
name = "typescript"
formatter = { command = 'prettier', args = ["--parser", "typescript"] }
auto-format = true

[[language]]
name = "tsx"
formatter = { command = 'prettier', args = ["--stdin-filepath", "file.tsx"] }
auto-format = true

# Deno's formatter is written in Rust and is very fast compared to Prettier,
# but it uses a different configuration file. Therefore, we still use Prettier.
# However, if you prefer, we can use Deno's formatter CLI options to enable
# similar configurations in Prettier. 
# For the CLI options, see https://deno.land/manual@v1.32.1/tools/formatter

# To use, install deno with brew
# [[language]]
# name = "javascript"
# formatter = { command = 'deno', args = ["fmt", "-", "--ext", "js" ] }
# auto-format = true

# [[language]]
# name = "json"
# formatter = { command = 'deno', args = ["fmt", "-", "--ext", "json" ] }

# [[language]]
# name = "markdown"
# formatter = { command = 'deno', args = ["fmt", "-", "--ext", "md" ] }
# auto-format = true

# [[language]]
# name = "typescript"
# formatter = { command = 'deno', args = ["fmt", "-", "--ext", "ts" ] }
# auto-format = true

# [[language]]
# name = "jsx"
# formatter = { command = 'deno', args = ["fmt", "-", "--ext", "jsx" ] }
# auto-format = true

# [[language]]
# name = "tsx"
# formatter = { command = 'deno', args = ["fmt", "-", "--ext", "tsx" ] }
# auto-format = true
